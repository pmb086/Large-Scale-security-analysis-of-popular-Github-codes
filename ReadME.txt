Libraries used for python:
Scrapy, sklearn, pandas

1) Run the scraper folder from anaconda prompt like:

scrapy crawl Githubcode

2) Replace the file 'Githubcode.py' with the file available outside to generate test data

3) Run df.py to generate the train data along with gathered ground truth file final_lgtm.csv in the same folder

4) Run the classification.py file along with test and train csv file

